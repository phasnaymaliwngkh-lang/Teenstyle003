/**
 * ข้อมูลร้านและนโยบายที่ไม่ใช่ตัวเงิน (STEP 20/21)
 *
 * ⚠️ นี่คือ **แหล่งความจริงเดียว** ของ เวลาทำการ · ช่องทางติดต่อ · เงื่อนไขเปลี่ยน/คืนสินค้า
 *    ทั้ง Policy Engine ของ AI Customer Service และบทความในคลังความรู้อ่านจากไฟล์นี้
 *
 *    เหตุผล: ตอนสร้าง STEP 20/21 ข้อมูลชุดนี้ถูกพิมพ์แยกกัน 2 ที่แล้วขัดกันเอง เช่น
 *      - เวลาทำการ: `ai-cs.service.ts` บอก จ–อา 09:00–21:00 · บทความบอก จ–ส 09:00–18:00
 *      - เวลาตัดรอบส่ง: policy บอก 12:00 · บทความบอก 14:00
 *    ลูกค้าถามคำถามเดียวกันสองทางแล้วได้คนละคำตอบ = ร้านดูไม่น่าเชื่อถือ
 *    และละเมิดกฎกลางข้อ 3 (AI ห้ามแต่ง Policy)
 *
 * ⚠️ **ห้ามใส่ช่องทางติดต่อที่ยังไม่มีจริง** — เดิมบทความระบุเบอร์ 02-999-8888
 *    ซึ่งเป็นเบอร์สมมติ ลูกค้าที่เดือดร้อนจริงโทรไปแล้วไม่มีใครรับ
 *    ช่องทางไหนยังไม่เปิดให้ตั้งเป็น `null` แล้วหน้าเว็บ/AI จะไม่พูดถึงเลย
 *    (แพตเทิร์นเดียวกับ STEP 16 ข้อ 6: ไม่สร้างช่องทางแจ้งเตือนที่ส่งไม่ได้จริง)
 *
 * STEP 44 (Shipping/Store Management) จะย้ายค่าเหล่านี้ไปเป็นข้อมูลในฐานข้อมูลให้ admin แก้เอง
 */

export interface StoreContactChannel {
  label: string;
  /** null = ร้านยังไม่เปิดช่องทางนี้ → ห้ามเอาไปแสดงหรือให้ AI พูดถึง */
  value: string | null;
  note?: string;
}

/** ช่องทางติดต่อ — เรียงตามลำดับที่อยากให้ลูกค้าใช้ */
export const STORE_CONTACT_CHANNELS: readonly StoreContactChannel[] = [
  {
    label: 'แชตกับฝ่ายบริการลูกค้าบนเว็บ',
    value: '/customer-service',
    note: 'AI ตอบทันที และส่งต่อให้เจ้าหน้าที่คนจริงได้ทุกเมื่อ',
  },
  {
    label: 'อีเมล',
    value: 'hello@teenstyle.ai',
  },
  {
    label: 'โทรศัพท์',
    value: null,
    note: 'ร้านยังไม่เปิดบริการทางโทรศัพท์',
  },
];

/** เวลาทำการของเจ้าหน้าที่คนจริง */
export const STORE_AGENT_HOURS = 'วันจันทร์ – เสาร์ เวลา 09:00 – 18:00 น.';

/** AI ตอบได้ตลอดเวลา — ข้อความนี้ใช้ร่วมกันทุกที่ */
export const STORE_AI_HOURS = 'ตลอด 24 ชั่วโมง ทุกวัน';

/** เวลาตัดรอบส่งของ */
export const STORE_CUTOFF_TIME = '12:00 น.';

/** วันทำการที่ร้านส่งของ */
export const STORE_SHIPPING_DAYS = 'จันทร์ – เสาร์';

/** จำนวนวันที่ลูกค้าแจ้งเปลี่ยน/คืนสินค้าได้ นับจากวันที่ขนส่งขึ้นสถานะ "ส่งมอบสำเร็จ" */
export const RETURN_WINDOW_DAYS = 7;

/**
 * โซนเวลาที่ใช้นิยาม "หนึ่งวันทำการของร้าน" (STEP 26)
 *
 * ⚠️ **แหล่งความจริงเดียว — ทั้งฝั่ง SQL และ TypeScript ต้องใช้ค่านี้ค่าเดียวกัน**
 *
 *    คอลัมน์เวลาของโปรเจกต์นี้เป็น `timestamp without time zone` (ค่าเริ่มต้นของ Prisma
 *    บน PostgreSQL) ซึ่งเก็บ **หน้าปัดเวลา UTC** ไว้ · `date_trunc('day', ...)` เปล่า ๆ
 *    จึงตัดวันแบบ UTC → ยอดขายของร้านไทยช่วง 00:00–07:00 น. ไปกองอยู่ใน "เมื่อวาน" ทุกวัน
 *    กราฟและยอดรายวันผิดทั้งแผ่นโดยไม่มีอะไรฟ้อง
 *    → ต้องแปลงสองทอดเสมอ: `"paidAt" AT TIME ZONE 'UTC' AT TIME ZONE ${STORE_TIME_ZONE}`
 *      (ทอดเดียวแปลผิดทาง เพราะคอลัมน์ไม่มีโซนติดมา)
 *
 *    ฝั่ง TypeScript ห้ามฮาร์ดโค้ด `+07:00` แม้ประเทศไทยจะไม่มี DST
 *    เพราะถ้าร้านย้ายโซนเวลา ค่าคงที่นั้นจะเพี้ยนเงียบ ๆ ในขณะที่ SQL ยังถูกอยู่
 *    ให้ใช้ `zonedDayStart()` / `zonedDayEnd()` / `utcTs()` ใน `models/analytics.model.ts`
 *    ซึ่งอ่านค่านี้และจัดการรายละเอียดข้างต้นให้แล้ว
 */
export const STORE_TIME_ZONE = 'Asia/Bangkok';

/** ช่องทางติดต่อที่เปิดใช้งานจริงเท่านั้น */
export function availableContactChannels(): StoreContactChannel[] {
  return STORE_CONTACT_CHANNELS.filter((channel) => channel.value !== null);
}
