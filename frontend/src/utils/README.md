# src/utils

ฟังก์ชันช่วยที่ไม่ผูกกับ React (pure function) — ต่างจาก `lib/` ที่เก็บตัวเชื่อมกับระบบภายนอก
เช่น api client, env, cn

ที่จะเพิ่ม: `format-currency.ts` (บาท), `format-date.ts` (พ.ศ./ไทย), `slugify.ts` (STEP 33 SEO URL),
`calculate-discount.ts` (STEP 41)
